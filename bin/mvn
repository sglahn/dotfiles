#!/usr/bin/env bash
#
# Runs Maven as Docker container
#
IMAGE_NAME=maven:3.5.2-jdk-8

docker run -it --rm \
    -v "$HOME"/.m2:/root/.m2 \
    -u `id -u $USER`:`id -g $USER` \
    -v "$PWD":/usr/src/mymaven \
    -w /usr/src/mymaven \
    $IMAGE_NAME \
    mvn $@
